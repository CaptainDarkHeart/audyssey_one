{
  "name": "audyssey-one",
  "version": "1.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch",
    "test": "node --test tests/*.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
